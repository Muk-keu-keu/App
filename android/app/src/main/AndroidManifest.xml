<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 위치 권한. 배달 가능한 매장·조합을 거르는 데 쓴다.
         FINE 과 COARSE 를 함께 선언한다 — Android 12+ 는 권한 팝업에서 사용자가
         "대략적인 위치"만 허용할 수 있고, 그때 COARSE 선언이 없으면 아무 좌표도 못 받는다.
         동네 수준 정확도면 충분한 기능이라 대략적인 위치만 허용해도 정상 동작한다. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- allowBackup 을 끈 이유: 로그인 토큰이 SharedPreferences 에 남는다. 백업이
         켜져 있으면(기본값 true) 그 파일이 클라우드 자동 백업과 `adb backup` 으로
         기기 밖으로 나간다. 앱이 백업할 만한 다른 데이터가 없어 통째로 끈다. -->
    <application
        android:label="먹방요기"
        android:name="${applicationName}"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- 공유 시트 대응.
                 iOS 는 별도 Share Extension 타깃과 openURL 우회가 필요했지만,
                 안드로이드는 이 선언만으로 공유 시트에 앱이 뜨고 탭하면 이 Activity 가
                 직접 실행된다. 링크는 Intent.EXTRA_TEXT 로 들어온다. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <!-- url_launcher 가 요기요 앱 설치 여부를 확인하려면 필요 (Android 11+) -->
        <package android:name="com.fineapp.yogiyo"/>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
