/// 이 파일을 lib/secrets.dart 로 복사하고 실제 키를 넣으세요.
/// secrets.dart 는 .gitignore 에 등록돼 있어 커밋되지 않습니다.
class Secrets {
  const Secrets._();

  /// https://aistudio.google.com/apikey 에서 발급
  static const geminiApiKey = 'YOUR_GEMINI_API_KEY';
}
