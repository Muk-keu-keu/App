/// 번들 에셋 경로. 모델·저장소·화면이 함께 쓰므로 아무것도 import 하지 않는다.
library;

/// 번들 이미지 경로.
class AppImages {
  const AppImages._();

  /// 사진이 없을 때 그리는 중립 자리.
  ///
  /// **특정 매장 로고를 이 자리에 쓰면 안 된다.** 예전에는 `store_dujjim.png`(두찜
  /// 간판)가 이 몫이었다. 그래서 사진 없이 쓴 글, 대표 이미지가 없는 서버 글,
  /// 로고 없는 매장, 영상 썸네일을 못 받은 주문까지 전부 두찜 간판을 달고 나왔다 —
  /// 두찜과 아무 상관 없는 내용에 그 브랜드가 붙는 것이라 잘못된 정보였다
  /// (피드백 2026-08-21). 빈 목록 상태와 같은 클로슈 글리프를 쓴다.
  static const placeholder = 'assets/images/placeholder_thumb.png';
}
