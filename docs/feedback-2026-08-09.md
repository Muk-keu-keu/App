# 디자이너 피드백 (2026-08-09)

출처: `피드백.pdf` — 구현 화면과 디자인 시안을 좌우로 놓고 단 주석 29건.
Figma 파일 `1HtvfSB8sq95TGsqs1LHw9`.

상태 표기
- `반영` — 코드에 적용하고 `flutter test` 로 확인함
- `Figma` — 시안 대조가 필요한 시각 항목. 치수·에셋을 받아야 고칠 수 있다
- `구조` — 화면 구성이 바뀌는 항목
- `확인` — 의도인지 버그인지 판단이 필요한 항목

## 요기요 홈 (681:6419)

| # | 피드백 | 상태 | 비고 |
| --- | --- | --- | --- |
| 1 | 배너 일러스트 위치가 어긋나고 그림자가 빠졌다 | Figma | `banner_platter.png` 위치·그림자 값 필요 |
| 2 | "먹방요기 바로가기" 를 누르면 먹방요기로 이동해야 한다 | 반영 | 문구만 있고 탭 대상이 없었다 |
| 3 | 간격이 원본과 다르다 | Figma | 어느 구간인지 지정 필요 |
| 4 | 아이콘 크기와 위치가 다르다 | Figma | 퀵메뉴·카테고리 대상 확인 필요 |
| 5 | 불 아이콘이 원본과 다르고 폰트 굵기가 얇다 | Figma | `fire.png` 교체 + 섹션 타이틀 weight |
| 6 | 홈 아이콘이 다르고 눌렀을 때 팝업 알림이 없다 | 아이콘 반영 | **먹방 조합 화면 헤더**의 홈이다 (하단 내비가 아니다). Material `home_outlined` → `icon/home`(`nav_home.svg`, 시안과 path 동일). 팝업 내용은 미정 |

## 요기족보 상세 (681:8066 계열)

| # | 피드백 | 상태 | 비고 |
| --- | --- | --- | --- |
| 7 | 점 아이콘이 뒤로가기와 같은 라인에 있어야 하는데 게시물 영역에 있다 | 반영 | `openPostMenu` 를 최상위로 올려 `DsHeader.detail(actions:)` 로 옮겼다. 아이콘도 스펙대로 지름 2 원 3개(간격 6, gray800)를 직접 그린다 — `Icons.more_vert` 는 점이 더 크다 |
| 8 | 좋아요를 누르면 채워져야 한다 (현재 라인만 색이 변함) | 반영 | `heart_fill.svg` 추가. 목록·상세 같은 아이콘으로 통일 |
| 9 | 댓글 전송 버튼: 입력 전 회색 → 입력 후 핑크. 화살표 크기도 다르다 | 반영 | 빈 입력에서는 눌리지 않는다 |
| 10 | '주문한 메뉴' 를 누르면 아코디언이 아니라 바텀시트가 떠야 한다 | 반영 | 상세에는 h62 한 줄만 두고(909:2578) 시트(893:1944)에서 조합을 보여준다. 시트는 금액·수량과 "나도 주문하기" CTA 를 갖는다 |
| 11 | 탭 각각의 사이즈가 원본과 다르다 | 반영 | 시안 850:4364 — 앞 두 탭만 108 고정, 마지막은 글자 폭. 바 자체의 하단 선도 추가. 라벨 길이에 맡겨 간격이 튀던 것을 고쳤다 |

## 주문내역 / 주문내역 상세 (857:5521, 857:4509)

| # | 피드백 | 상태 | 비고 |
| --- | --- | --- | --- |
| 12 | "작성한 족보" → "족보 작성" | 반영 | 이미 쓴 결제도 같은 라벨. 구분은 색으로만 |
| 13 | 원본에서 생략된 부분이 많다 | 확인 | 어느 요소인지 지정 필요 |
| 14 | 주문내역·주문내역 상세가 시안과 다르다 | Figma | 미대조 화면. `menuSummary` 서버 미제공 항목과 겹침 |
| 15 | 조합을 공유했을 때 토스트 알림이 뜨지 않는다 | 반영 | 화면이 먼저 사라져 messenger 를 못 찾던 문제 |

## 조합 결과 / 비교 (925:4220)

| # | 피드백 | 상태 | 비고 |
| --- | --- | --- | --- |
| 16 | "n개 중 n개 선택" 이 생략되어 있다 | 반영 | 시안 925:4305. 개수만 14 SemiBold primary500, 나머지 12 Regular |
| 17 | 체크 해제가 되지 않는다 | 반영 | `DsCheckbox(isOn: true)` 가 고정이고 `onTap` 이 없었다. 체크가 곧 담기이므로 `toggleSuggestionInCart` 로 잇고 미선택 카드에도 빈 체크박스를 그린다 |
| 18 | 선택했을 때 카드의 라인 색이 적용되지 않는다 | 반영 | 테두리가 "현재 페이지"를 따라 항상 하나만 켜졌고 미선택은 투명이었다. 선택 primary400 / 미선택 gray300 (시안 925:4225·925:4251) |
| 19 | 레이어 블러가 프로그레시브로 위로 갈수록 사라져야 한다 | 반영 | Flutter 에 프로그레시브 블러가 없어, 흐린 사본을 위→아래 그라디언트로 마스킹해 겹친다. 어둡게 덮는 층도 같은 그라디언트다 |
| 20 | 휴지통 아이콘을 누르면 그 메뉴가 삭제되어야 한다 | 반영 | 수량 1에서 한 번 더 내리면 줄이 빠진다 |
| 21 | "옵션 변경" 선택 시 바텀시트가 떠야 한다 | 반영됨 확인 | 조합 카드·장바구니 모두 `MenuOptionSheet.show` 로 연결돼 있다. 본문을 `MenuOptionList` 로 빼면서 함께 점검했다 |
| 22 | 밑에 선이 검정색으로 되어 있다 | 대상 확인 필요 | 탭 밑줄 `gray800` 2px 은 **시안 850:4364 그대로**였다. 옵션 시트·주소 시트에도 검정 선이 없다(전부 gray300). 어느 선인지 지정 필요 |

## 매장 메뉴 / 옵션 (925:4037 계열)

| # | 피드백 | 상태 | 비고 |
| --- | --- | --- | --- |
| 23 | 뒤로가기 버튼이 원본과 다르고 위치도 다르다 | 반영 | 메뉴 상세의 사진 위에 얹은 반투명 흰 원형(왼쪽 20, 상태바 아래 2, padding 8 + chevron 20) |
| 24 | 가게 명이 안 뜬다 | 서버 반영으로 해소 | `GET menus` 가 2026-08-09 부터 `restaurant` 를 준다. 앱은 그 값으로 이름을 그린다 (b612906) |
| 25 | 메뉴를 누르면 상세 화면이 나와야 하는데 안 뜬다 | 반영 | `MenuDetailScreen` 신설 (`AppStage.menuDetail`). 목록의 + 는 옵션 없이 담는 빠른 길로 남기고, 카드를 누르면 옵션을 고르고 담는다. 옵션 본문은 옵션 변경 시트와 `MenuOptionList` 로 공유한다 |
| 26 | "주문하기" 를 "장바구니" 로 바꾼 것이 의도인지 | 확인 | 시안 라벨 확정 필요 |
| 27 | close 아이콘이어야 하는데 플러스 아이콘으로 되어 있다 | 미반영 | |
| 28 | 아예 다른 매장 사이에는 갭이 필요하다 | 미반영 | 장바구니 매장 구분 |
| 29 | 결제완료 화면이 시안과 다르다 | 반영 | 시안 949:4470 대로 다시 그렸다 — success icon 60, "결제가 완료되었습니다", 금액 카드(gray100/radius16/p20, 결제 금액만 primary500), 버튼은 "홈으로 이동하기" 하나. 금액은 앱이 계산한 값이다 (201 응답에 금액이 없다) |

## Figma 대조 결과 (2026-08-09)

캔버스 `681:5931` = `04_최종화면`. 아래 치수는 Figma 에서 직접 읽은 값이다.

### 족보 상세 `909:2540` (390x937)

| 자식 | 노드 | 위치·크기 |
| --- | --- | --- |
| Status Bar | 909:2584 | y 0, h 53 |
| header | 909:2583 | y 53, h 56 |
| post section | 909:2542 | y 109, h 371 |
| **ordered menu row** | 909:2578 | y 480, h 62 |
| comment section | 909:2573 | y 558, h 345 |
| comment composer | 909:2581 | y 841, h 96 |

헤더 `909:2583` 내부: 배경 흰색, padding 좌우 20 / 상하 12.
- `left_area` 64x32 — `icon/chevron` 20x20
- `center_area` flex, 제목 비어 있음 (H3 / Pretendard SemiBold 20 / letterSpacing -0.4)
- `right_area` 64x32, 우측 정렬, gap 12 — 아이콘 24x24

**점 3개 아이콘의 실제 형태** (Figma 이름은 `icon/bell` 이지만 종 모양이 아니다):
24x24 박스 안에 지름 2 원 3개를 세로로, 중심에서 -6 / 0 / +6 위치. 색 `gray800`.
7번 피드백대로 이 아이콘은 `right_area` 에 있어야 한다. 현재 코드는
`post_detail_screen.dart:169` 에서 작성자 줄에 `Icons.more_vert` 20 / gray600 으로 그린다.

`ordered menu row` `909:2578` 은 아코디언이 아니다:
- 텍스트 "주문한 메뉴" x 20, y 20 (72x22)
- `icon/chevron` x 350, y 21 (20x20) — 오른쪽 꺾쇠

### 주문한 메뉴 상세 `893:1944` (390x780) — 10번이 요구한 바텀시트

| 자식 | 노드 | 위치·크기 |
| --- | --- | --- |
| Drag Handle | 893:1997 | x 171, y 12 — 48x5 |
| header | 893:1996 | y 32, h 56 |
| cart list | 902:1332 | y 104, h 493 (cart item 370x295, 370x182) |
| Bottom CTA | 900:1256 | y 676, h 104 — Button 350x52 at (20, 20) |

### 먹방 조합 `925:4220` (2026-08-12 실측)

| 요소 | 노드 | 값 |
| --- | --- | --- |
| 프레임 배경 | 925:4220 | `bg` #F2F2F2 |
| header | 925:4309 | 흰 배경, px 20 / pt 68 / pb 16. title area 299 wide |
| 제목 | 925:4311 | 28 SemiBold, tracking -0.7 |
| 부제 | 925:4312 | 16 Regular, gray700 |
| 홈 아이콘 | 584:1972 | `icon/home` type=line 24x24. `nav_home.svg` 와 path 동일 |
| 선택 개수 | 925:4305 | x 21, y 161. "N개 중" 12 Regular + "M개" 14 SemiBold primary500 + "선택" 12 Regular |
| content area | 925:4223 | x 21, y 197, w 350, 카드 간 gap 12 |
| card (선택) | 925:4225 | 350x520, radius 16, **border 2 primary400** |
| card (미선택) | 925:4251 | 같은 크기, **border 2 gray300** |
| checkbox | 925:4250 / 925:4274 | 32x32 at (300, 14) → 우측 18. 켠 상태 primary500, 끈 상태 흰 배경 + gray500 1.5 |
| store area | 925:4226 | h 152, 레이어 블러 30 (프로그레시브 — 위로 갈수록 사라진다) |
| divider | 925:4244 | gray300 1px |

### 메뉴 추가하기 `925:4037` (2026-08-12 실측) — 25번이 요구한 화면

**바텀시트가 아니라 전체 화면이다.** 앱에는 아직 이 화면이 없다 (옵션 변경
바텀시트만 있다).

| 요소 | 노드 | 값 |
| --- | --- | --- |
| store image | 925:4090 | y 0, h 202, 좌우 꽉 채움 |
| **뒤로가기** | 925:4091 | x 20, y 55. `rgba(255,255,255,0.8)` 원형(radius 100), padding 8, `icon/chevron` 20 → 버튼 36x36 |
| 메뉴명 | 925:4200 | x 20, y 215. H2 24 SemiBold |
| 설명 | 925:4201 | 14 Regular gray600 |
| Main Container | 925:4127 | y 314, px 20 |
| 옵션 그룹 제목 | 925:4130 | sub2 16 SemiBold gray800 |
| 옵션 줄 | 925:4132 | radio 20 + 라벨 14 Regular gray800, 가격 14 Regular gray600 우측 |
| 그룹 padding / 옵션 간격 | 925:4128 | py 20 / gap 12 (그룹 제목과 목록 사이 16) |
| divider | 925:4142 | gray300 1px |
| Bottom CTA | 925:4203 | 흰 배경, pt 20 / pb 32 / px 20, radius top 16, 버튼 문구 "추가하기" |

23번(뒤로가기 버튼)은 이 화면 기준으로 위 값을 쓰면 된다. 매장 메뉴 목록 화면의
뒤로가기는 별도 노드 대조가 필요하다.

### 그 밖에 확인한 프레임

`925:4220` 먹방 조합(신), `925:4037` 메뉴 추가하기(신), `949:4470` 결제 완료,
`857:4933` 주문내역/상세보기, `850:4328` 주문내역(Tab Bar `850:4364` — tab 108x40, 간격 13),
`952:5089` toast, `922:2734` overflow menu (120x36, 삭제하기 + `icon/delete` 20x20).

`850:4364` Tab Bar 로 11번(탭 사이즈)을 맞출 수 있다: tab 108x40 세 개, x 20 / 141 / 262.

## 디자이너에게 확인이 필요한 것

- 3번 "간격", 4번 "아이콘", 13번 "생략된 부분" 은 대상이 특정되지 않았다
- 6번 홈 아이콘 팝업 알림의 내용과 조건
- 26번 하단 CTA 라벨 확정 ("주문하기" / "장바구니")
- 22번 "밑에 선" 이 어느 선인지 — 탭 밑줄은 시안대로 gray800 이고, 후보 화면에 검정 선이 없다
- 29번 완료 화면의 금액: 서버가 201 에 확정액을 주지 않아 앱 계산값을 보여준다.
  응답에 `totalPrice`·`deliveryFee` 를 넣어 주면 확정액으로 바꿀 수 있다
- 29번 완료 화면에서 "주문 내역 보기" 가 빠졌다 (시안에 버튼이 하나뿐). 주문내역은
  홈 하단 탭으로 간다 — 이대로 둘지 확인
